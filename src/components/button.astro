---
const { text, href, size } = Astro.props;
---

<a
  href={href}
  class={`mt-6 hidden h-fit w-fit border border-primary p-3 hover:bg-primary lg:flex ${
    size ? size : "text-xs"
  }`}>{text}</a
>
